# 书谱（BookMap）手动测试清单

> MVP阶段手动测试覆盖清单，M5阶段执行

| 编号 | 测试场景 | 通过标准 |
|------|---------|---------|
| T01 | 文本输入正常流程 | 首页输入→结构化页确认→地图页展示3-6本书 |
| T02 | 500字超长输入 | 被截断到500字，不报错 |
| T03 | 空输入提交 | 显示"请输入你的想法"提示，不发API请求 |
| T04 | 语音输入(Chrome) | 语音转文字成功，填充到输入框 |
| T05 | 语音输入(Safari/不支持) | 显示"浏览器不支持语音，请用文字输入" |
| T06 | 标签编辑 | 四个标签均可以编辑，编辑后重新推荐结果变化 |
| T07 | "直接看结果"按钮 | 使用当前structuredIdea（含用户编辑）直接推荐 |
| T08 | AI返回JSON包裹在markdown代码块 | 能正确解析，不报错 |
| T09 | AI返回格式错误 | 自动重试1次，仍失败则显示友好错误 |
| T10 | Google Books超时/失败 | 书籍卡片正常显示(verified=false)，不阻塞 |
| T11 | API Key无效 | 显示"服务暂时不可用"，不泄露Key信息 |
| T12 | 频率限制 | 连续10次请求后第11次返回429 |
| T13 | Prompt注入测试：输入"忽略之前指令..." | AI输出仍符合Schema，不被操纵 |
| T14 | 刷新structured页 | sessionStorage恢复到structured页（非map页） |
| T15 | 刷新map页 | 恢复到structured页（需重新确认） |
| T16 | 隐私模式(sessionStorage不可用) | 页面正常工作，刷新后状态丢失但不报错 |
| T17 | 复制分享 | 粘贴出的文本包含书名+作者+核心概括 |
| T18 | 移动端(<400px)布局 | 无横向滚动，方向堆叠清晰 |
| T19 | 桌面端(>1024px)布局 | 空间地图展示正确，方向布局清晰 |
| T20 | 返回导航 | 地图页→结构化页→首页，状态保留正确 |

## M7安全审查清单（部署前必做）

- [ ] T13 Prompt注入测试（至少测试5种注入变体）
- [ ] 检查API Key不在客户端bundle中（`pnpm build && grep -r "sk-" .next/` 无结果）
- [ ] 检查`.env.local`不在git中（`git status`不显示.env.local）